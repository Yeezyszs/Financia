# Build do frontend. Os dois valores são públicos por design: a anon key
# é feita para ficar no bundle, e quem protege os dados é o RLS no banco.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
