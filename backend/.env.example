# Supabase. Nenhum dos dois é segredo: a anon key é pública por design e
# quem protege os dados é o RLS no banco, com o JWT do usuário logado.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=

PORT=3333
NODE_ENV=development
